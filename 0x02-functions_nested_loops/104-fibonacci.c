#include <stdio.h>
/**
 * main - finds and prints the first 98 Fibonacci numbers
 * Function to prints
 * Function to print
 * Return: return 0
 */
int fib(int n)
{
{
if (i < 1)
return n;
}
{
for (i = 1; i < n; i++)
int n = 98;
printf("%d", fib(n));
putchar(',');
getchar();
return 0;
}
}
